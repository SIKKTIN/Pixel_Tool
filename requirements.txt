# PerfectPixelTool 桌面端依赖
# ============================================================
# 核心 (像素细化 / 尺寸缩放)
# ============================================================
numpy>=1.20.0
opencv-python>=4.8.0
Pillow>=10.0.0
PySide6>=6.5.0

# ============================================================
# 可选:去水印模块 (SLBR + LaMa)
#   如不需要去水印功能,可注释掉本节
# ============================================================
torch>=2.0.0
torchvision>=0.15.0
scipy>=1.11.0
loguru>=0.7.0
